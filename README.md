## Iniciar Proyecto

Crear entorno virtual
```shell
pipenv shell
```

Instalar dependencias
```shell
pipenv install
```

iniciar server (windows)
```shell
python app.py
```

iniciar server (mac or linux)
```shell
python3 app.py
```